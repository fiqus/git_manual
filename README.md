# GIT - Manual de usuarios

## Build

`make` o `make -f Makefile`. 

### Dependencias

* Make
* [Pandoc](https://github.com/jgm/pandoc)
* xelatex

#### Linux (Debian based):

<code>apt-get install pandoc texlive-latex-base texlive-fonts-recommended cm-super etoolbox texlive-xetex</code>
